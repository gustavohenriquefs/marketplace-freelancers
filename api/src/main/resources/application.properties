spring.application.name=api
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres

spring.jpa.hibernate.ddl-auto=update
# Dica: Em vers�es novas do Spring Boot, essa linha abaixo � opcional (ele detecta sozinho), mas n�o faz mal deixar.
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# --- Configura��o do Swagger ---
# For�a o scan no seu pacote
springdoc.packages-to-scan=com.ufc.quixada.api

# --- Seguran�a ---
api.security.token.secret=mysecretkeycaraio

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.com.ufc.quixada.api=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE